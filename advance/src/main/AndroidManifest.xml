<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="shine.com.advance">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".acivity.MainActivity" />
        <activity android:name=".messenger.MessengerActivity" />
        <activity android:name=".service.StartServiceActivity" />
        <activity android:name=".service.LocalActivity" />

        <service
            android:name=".aidl.RemoteService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />
        <service
            android:name=".messenger.MessengerService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote">
            <intent-filter>
                <action android:name="shine.com.advance.IService" />
            </intent-filter>
        </service>

        <activity android:name=".aidl.FactoryActivity" />

        <service
            android:name=".aidl.FactoryService"
            android:enabled="true"
            android:exported="true"
            android:process=":remote" />
        <service android:name=".service.LocalService" />

        <activity android:name=".aidl.RemoteActivity" />
        <activity android:name=".acivity.HomeActivity"></activity>
        <activity android:name=".acivity.AnimationActivity">


        </activity>
    </application>

</manifest>